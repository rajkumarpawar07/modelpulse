Create the two demo-video deliverables for ModelPulse (Scrape-Verse submission):

## 1. PowerPoint deck — `docs/modelpulse-demo-deck.pptx`

7 slides, ~2 minutes of narration, built with pptxgenjs (dark mission-control theme matching the dashboard so the PPT flows visually into the live demo: near-black `0A0E12` background, signal-green `35F0B4` accent, Courier New HUD-style section labels, corner-bracket motif, EKG pulse line on the title slide; safe fonts only — Arial + Courier New):

1. **Title** — ModelPulse, AI API Change Intelligence; Scrape-Verse × Bright Data Scraper Studio; EKG polyline drawn with line shapes (matches the sidebar logo)
2. **The Problem** — devs find out about breaking changes from error logs; big stat callouts; the 4-point "why not just RSS?" answer (no feeds for half the vendors, unstructured prose, no silent-edit detection, feeds can't fail CI)
3. **The Solution** — 10 self-healing Bright Data collectors → one normalized schema → alerts/feeds/API/CI gate; differentiators grid (impact scoring, silent-edit ↻ detection, keyword watches)
4. **Architecture** — flow diagram drawn with shapes: GitHub Actions cron → 10 `c_*` collectors (Bright Data marked) → normalizer → SQLite → diff engine → Slack/Discord/webhooks; dashboard reads the same DB; DB committed back daily for persistence
5. **Where & How Bright Data Is Used** (the emphasized slide) — the three CLI commands mapped to real endpoints: create (`/dca/collector` + `automate_template`), run-as-API (`POST /dca/trigger` → `/dca/dataset`), heal loop (`refactor_template {prompt}` → `progress` gate → `resume_automation_job` approve); "every scraper is instantly an API"
6. **Self-Healing — Real Proof** — timeline of the actual event from 2026-08-22: Fireworks `c_mt36bqzoxmjmuuk6y` returned 29/29 rows with null `change_type` → detected → healed → approved (`ia_mt3bwk3f1l3wyn63hq`) → 29 rows recovered, same `c_*` ID, no code change; plus circuit breaker + partial-breakage detection callouts
7. **Closing / transition** — "The scraper that fixes itself" + repo/dashboard links → "let's watch it live"

Build script kept at `docs/ppt/build-deck.js`; output recompressed (pptxgenjs writes uncompressed zip); verified by unzipping and checking slide XML.

## 2. Demo video script — `docs/demo-video-script.md`

Timed 5:00 script in two parts:

- **0:00–3:00 PPT narration** — exact lines per slide (hook, problem, solution, architecture walk, Bright Data deep-dive, the Fireworks heal proof, transition)
- **3:00–5:00 live demo** — precise click path: terminal showing the real `npm run scrape` output scrolled to the heal section (safer than a live 5-min scrape), then the dashboard: overview (ticker, IMPACT/EDITED badges) → vendor page → timeline → stats → **/health as the star** (collector status + APPROVED heal log entry with the real interaction ID) → `/api/changes?breaking=true` JSON API → RSS feed → closing line
- Written against `localhost:3000` with a highlighted note to swap in the deployed URL (noting modelpulse.vercel.app currently serves an unrelated old project and must be redeployed first), plus recording tips (1080p, 15–20s margin, mask the API key)

Then commit both files.